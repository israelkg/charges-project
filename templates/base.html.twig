<!DOCTYPE html>
<html lang="pt-BR">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Bem-vindo!</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.2em%22 x=%220.1em%22 font-size=%2296%22 fill=%22%23fff%22>⚫️</text></svg>">
        
        <script src="https://cdn.tailwindcss.com"></script>
        <script src="https://unpkg.com/imask"></script>
        <script src="https://cdn.jsdelivr.net/npm/cleave.js@1/dist/cleave.min.js"></script>

        <style>
            body { font-family: 'Inter', sans-serif; }
            {# ul li {
                color: red;
            } #}

            @keyframes slide-in {
                from {
                    transform: translateY(-10px);
                    opacity: 0;
                }
                to {
                    transform: translateY(0);
                    opacity: 1;
                }
            }
            .animate-slide-in {
                animation: slide-in 0.4s ease-out;
            }
        </style>

    </head>
    <body class="bg-blue-950 text-gray-100 overflow-x-hidden"> 
        <div class="flex min-h-screen w-full">
            <aside class="w-64 bg-blue-900 shadow-xl flex flex-col justify-between fixed left-0 top-0 bottom-0 z-40">
                <div>
                    <div class="p-6 text-2xl font-extrabold text-white border-b border-blue-800 tracking-wide">
                        <a href="{{ path('app_charges_list') }}" class="hover:text-blue-300 transition">Cobranças</a>
                    </div>

                    <nav class="mt-6 px-4 text-base space-y-2 font-medium text-blue-100">
                        <a href="{{ path('app_dashboard') }}" class="block px-3 py-2 rounded-lg hover:bg-blue-800 transition duration-200"> 📊 Dashboard </a>
                        <a href="{{ path('app_charges_list') }}" class="block px-3 py-2 rounded-lg hover:bg-blue-800 transition duration-200"> 💼 Minhas Cobranças </a>
                        <a href="{{ path('app_charges_new') }}" class="block px-3 py-2 rounded-lg hover:bg-blue-800 transition duration-200"> ➕ Nova Cobrança </a>
                    </nav>
                </div>

                <div class="p-5 border-t border-blue-800 bg-blue-950 text-blue-100 text-sm">
                    {% if app.user %}
                    <p class="mb-1 text-xs text-blue-300">Logado como:</p>
                    <p class="font-semibold text-white truncate">{{ app.user.name ?? app.user.email }}</p>
                    <div class="mt-3">
                        <a href="{{ path('app_logout') }}"
                           class="inline-block bg-red-600 hover:bg-red-700 text-white text-xs font-semibold px-4 py-1.5 rounded-lg transition duration-200">
                            Sair
                        </a>
                    </div>
                    {% else %}
                        <a href="{{ path('app_login') }}"
                           class="inline-block bg-blue-700 hover:bg-blue-800 text-white text-xs font-semibold px-4 py-1.5 rounded-lg transition duration-200">
                            Login
                        </a>
                    {% endif %}
                </div>
            </aside>


            <div class="ml-64 w-full px-6 py-6">
                <div id="flash-container" class="fixed top-6 left-1/2 transform -translate-x-1/2 z-50 space-y-4 w-full max-w-md px-4">
                {% for label, messages in app.flashes %}
                    {% for message in messages %}
                        <div class="flash-message animate-slide-in opacity-100 transition-opacity duration-500 px-5 py-4 rounded-lg shadow-lg border 
                            {% if label == 'success' %}
                                bg-green-50 border-green-200 text-green-800
                            {% elseif label == 'error' %}
                                bg-red-50 border-red-200 text-red-800
                            {% elseif label == 'warning' %}
                                bg-yellow-50 border-yellow-200 text-yellow-800
                            {% else %}
                                bg-blue-50 border-blue-200 text-blue-800
                            {% endif %}
                            ">
                            <div class="flex items-start justify-between gap-4">
                                <div class="flex items-center gap-3">
                                    {% if label == 'success' %}
                                        <svg class="w-6 h-6 text-green-600" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                                            <path d="M5 13l4 4L19 7" stroke-linecap="round" stroke-linejoin="round"/>
                                        </svg>
                                    {% elseif label == 'error' %}
                                        <svg class="w-6 h-6 text-red-600" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                                            <path d="M6 18L18 6M6 6l12 12" stroke-linecap="round" stroke-linejoin="round"/>
                                        </svg>
                                    {% elseif label == 'warning' %}
                                        <svg class="w-6 h-6 text-yellow-600" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                                            <path d="M12 9v2m0 4h.01M5.07 19h13.86A2 2 0 0020.92 17L12 3 3.08 17a2 2 0 001.99 2z" stroke-linecap="round" stroke-linejoin="round"/>
                                        </svg>
                                    {% else %}
                                        <svg class="w-6 h-6 text-blue-600" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                                            <path d="M13 16h-1v-4h-1m1-4h.01M12 2a10 10 0 1010 10A10 10 0 0012 2z" stroke-linecap="round" stroke-linejoin="round"/>
                                        </svg>
                                    {% endif %}
                                    <p class="text-sm font-medium">{{ message }}</p>
                                </div>
                                <button onclick="this.closest('.flash-message').remove()" class="text-gray-400 hover:text-gray-700 transition text-xl leading-none">&times;</button>
                            </div>
                        </div>
                    {% endfor %}
                {% endfor %}
            </div>

            <main>
                {% block body %}{% endblock %}
            </main>
        </div>
    </div>

        <script src="{{ asset('js/utils/utils.js') }}"></script>    
    </body>
</html>